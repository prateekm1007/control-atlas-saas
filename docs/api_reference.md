# Toscanini API Reference — Phase B1

## POST /refinement/callback
Accept refined structure and trigger re-audit.

## GET /comparison/{original_id}/{refined_id}
Retrieve stored comparison metadata.

## GET /health
System health check.
